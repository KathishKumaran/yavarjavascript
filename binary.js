function addBinary(a, b) {
  let number = a + b;
  let binary = []; 
  arr = number.toString().split('');
  console.log(arr);
  for(i = arr.length; i >0;i--){
      if(arr[i -1] != 0){
          binary.push()
      }
  }
}

addBinary(10, 10);
